import type { Report } from '@/core/types';

/* ============================================================================
   REPORTES DE CONTENIDO
   ----------------------------------------------------------------------------
   Arranca vacío a propósito.

   Los reportes los genera la comunidad al marcar una publicación, así que no
   corresponde inventar ninguno de ejemplo: la cola debe reflejar lo que la
   gente reportó de verdad.

   Hoy no hay ningún módulo con contenido publicado por estudiantes, de modo
   que esta lista se mantendrá vacía hasta que exista uno. La maquinaria de
   moderación queda intacta y lista para ese momento.
   ========================================================================== */

export const seedReports: Report[] = [];
